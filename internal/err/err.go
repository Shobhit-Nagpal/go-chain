package err

const TXN_EXISTS_IN_BLOCK = "Transaction already exists in block"
const TXN_NONEXISTENT = "Transaction with given id does not exist"

const BLOCK_EXISTS_IN_CHAIN = "Block with the particular hash already exists in the chain"
const BLOCK_NONEXISTENT = "Block with given hash does not exist"

const NOT_ENOUGH_BALANCE = "The account doesn't have enough balance"
